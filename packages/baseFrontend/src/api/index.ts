import example from './example';

/**
 * ! api 를 추가할 때 마다 ApiType 을 직접 추가!!
 * ! name 값을 통해 아래와 같은 기본 ApiType 을 사용!!
 * ? {name}/add
 * ? {name}/fetchAll
 * ? {name}/fetchOneById
 * ? {name}/updateById
 * ? {name}/removeById
 * ? {name}/fetchCount
 */

export default {
  example,
};
